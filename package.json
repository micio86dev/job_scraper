{
  "name": "job_scraper",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "husky": "^9.1.7"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "prepare": "husky",
    "lint": "./venv/bin/flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics --exclude=venv",
    "test": "./venv/bin/python -m pytest"
  },
  "version": "0.1.0"
}